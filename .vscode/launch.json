{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Operator",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go",
            "env": {
                "KUBECONFIG": "${env:HOME}/.kube/config",
                "GITHUB_PRIVATE_KEY_PATH": "${workspaceFolder}/github-privateKey.pem"
            },
            "envFile": "${workspaceFolder}/.env",
            "preLaunchTask": "Deploy debug cluster"
        },
    ]
}
